# BIE v1.5 — Book → Scene Intelligence Pipeline

M9 foundation.

Run the example:
`python examples/run_example.py`

The output contains:
- Book IR
- question coverage matrix
- learning graph
- scene plan

This is the architecture contract, not a claim that the included baseline classifier can understand every book by itself. Production understanding requires layout-aware extraction + structured model reasoning + evidence/verification.
