# BIE v2.2 — Remotion Scene Renderer Contract

M16 defines the renderer-neutral Scene DSL and Remotion timeline manifest.

Run:
`python src/build_renderer_contract.py examples/lesson.json examples/renderer-contract.json`

This is deliberately not the final React code. It is the stable contract between the AI lesson planner and the Remotion renderer.
