# BIE v7.29 — M282 Book Structure Compiler

M282 turns layout-aware Document IR into a deterministic hierarchical book structure that downstream knowledge, planning and production stages can consume.

It is designed to make long-book processing a first-class artifact rather than a collection of unrelated pages.
