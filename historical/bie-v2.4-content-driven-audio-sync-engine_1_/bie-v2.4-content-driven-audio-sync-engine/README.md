# BIE v2.4 — Content-Driven Audio Sync Engine

M18 establishes audio-authoritative, content-driven timing.

There is no fixed maximum scene duration. Estimated timing is replaced by measured TTS/recorded audio duration before final render.

Run:
`python src/build_sync_contract.py examples/renderer-contract.json examples/audio-sync.json`
