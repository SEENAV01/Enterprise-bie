# BIE v7.42 — M295 Auto Repair + Re-render + Regression

M295 adds grounded repair application and the contract for a second render/regression cycle. It never reports a second render unless one actually occurs.
