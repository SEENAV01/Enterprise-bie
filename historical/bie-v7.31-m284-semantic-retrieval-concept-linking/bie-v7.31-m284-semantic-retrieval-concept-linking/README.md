# BIE v7.31 — M284 Semantic Retrieval & Concept Linking

M284 adds a retrieval layer over the grounded knowledge and book structure.

The implementation starts with deterministic TF-IDF retrieval and evidence-backed cross-section concept links. It deliberately leaves the retrieval backend replaceable for later embeddings/ANN/hybrid search.
