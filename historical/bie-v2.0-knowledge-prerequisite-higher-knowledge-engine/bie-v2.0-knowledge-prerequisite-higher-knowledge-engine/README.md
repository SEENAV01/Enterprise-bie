# BIE v2.0 — Knowledge Graph + Prerequisite + Higher Knowledge Engine

M14.

Run:
`python src/build_demo.py`

The output demonstrates:
- typed knowledge nodes
- provenance/status
- prerequisite edges
- learning path
- separate higher-knowledge candidate

The key design principle is epistemic separation:
SOURCE_DERIVED != INFERRED != EXTERNAL != SPECULATIVE.
