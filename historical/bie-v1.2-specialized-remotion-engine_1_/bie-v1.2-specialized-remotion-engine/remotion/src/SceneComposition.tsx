import React from "react"; import {AbsoluteFill,Easing,interpolate,useCurrentFrame} from "remotion"; import type {Scene} from "./types"; import {AnimatedText} from "./components/AnimatedText"; import {SpecializedVisual} from "./components/SpecializedVisual";
export const SceneComposition:React.FC<{scene:Scene}>=({scene})=>{const f=useCurrentFrame();const opacity=interpolate(f,[0,20],[0,1],{extrapolateRight:"clamp",easing:Easing.bezier(.16,1,.3,1)});return <AbsoluteFill style={{padding:70,opacity,display:"flex",flexDirection:"column",gap:35}}><AnimatedText fontSize={46}>{scene.title??scene.visual.type}</AnimatedText><div style={{flex:1,display:"flex",alignItems:"center",justifyContent:"center"}}><SpecializedVisual type={scene.visual.type} formula={scene.visual.formula} objects={scene.visual.objects}/></div><div style={{fontSize:26,maxWidth:1500}}>{scene.narration.key_points.join(" • ")}</div></AbsoluteFill>};
