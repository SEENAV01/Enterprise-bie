# M301 — Render Target Fix

The stale demo target `BIELesson` is replaced by the actual Lesson 1 composition `ElectricChargeLesson`.

Canonical command:

`npm run render`

Output:

`out/lesson1.mp4`

This is a configuration correction; it does not claim that dependencies are installed or that an MP4 has been rendered.
