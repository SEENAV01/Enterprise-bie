# BIE v2.1 — Adaptive Lesson Planner + Scene DSL

M15 converts knowledge graph nodes into learning units and renderer-neutral scenes.

Run:
`python src/build_lesson.py examples/lesson-input.json examples/lesson-output.json`

The output is intentionally NOT Remotion code. It is the pedagogical intermediate representation that the Remotion renderer will consume.
