# BIE v2.3 — Remotion Component Registry

M17 establishes the real renderer boundary.

The Scene DSL selects reusable React/Remotion components instead of generating arbitrary React per scene.

The included example can be imported into a Remotion project and used as the composition contract.
