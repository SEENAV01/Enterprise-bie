def semantic_profile(subject,features,learning_objective,
                     audience="general"):
    return {"subject":subject,"features":features,
            "learning_objective":learning_objective,
            "audience":audience}
