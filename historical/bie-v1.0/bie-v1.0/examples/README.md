Put a PDF in this folder or pass any PDF path to:

`python -m bie.cli path/to/book.pdf --out artifacts`

Requires `OPENAI_API_KEY` in the environment. Copy `.env.example` to `.env.local` and set it locally; never commit the secret.
