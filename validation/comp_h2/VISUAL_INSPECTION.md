# H2 component visual inspection

Actual Chromium output was inspected, not merely a process exit code. The first equation image showed a clipped radical/descender; the padded view box was regenerated and rechecked. The final contact sheet shows a typeset fraction/radical, native MathML, three analytic model trajectories and sampled markers, motion samples and two distinct projected map geometries. DOM glyph/label bounds and motion-marker/style changes supplement the image inspection.

An earlier oversized moving-child fixture clipped at the viewport. The final bounded fixtures are deliberately sized/positioned to fit; this does not implement a general scene-level motion-envelope guard. That production gap remains H2-R01. The reveal fixture was enlarged to span its clipping window after the earlier child was already fully visible by the midpoint.

Only browser_run_003 is the final component evidence. These are synthetic component checks with an explicit test React/Remotion bridge, not actual Remotion frames, a completed video, whole-scene lesson QA, subject-expert review or product acceptance.
