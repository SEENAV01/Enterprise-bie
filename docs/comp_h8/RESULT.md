# COMP Hardening H8 — verified-scope delivery

## Scope and lineage
Five bounded tasks extend inherited H7 findings R01/R02. Parent archive `BIE_COMP_HARDENING_H7_INTEGRATED.zip`, SHA256 `59835eb595d6cf1e0e5c4b3c3ba1ad252aae41e2cf7c83b3605ca824857ecddc`. All 5,121 inherited members are preserved or their exact originals are retained in lineage. Current integrated package is DSL+COMP, not the canonical enterprise repository. No GitHub changes.

## Implemented work
1. All-output-pixel counterfactual contribution plus connected-component diagnostics. Tiny marks are not discarded in a global average. PNG bounds, byte identity, profile and opaque-composite contracts fail closed.
2. Source-layer-bound map feature resolution: collapsed/subpixel geometry, inseparable points and largely coincident routes. The checker does not invent a basemap or remove source data.
3. Every-frame full/isolated/baseline/muted/repeat capture with reversible browser visibility, source-driven intentionally empty frames, exact source/image identity and reproducibility checks.
4. Live DSL registry-to-dispatch coverage with actual missing-name reproductions. Inventory is NOT implementation of those consumers.
5. Original-Scene actual Remotion capture/controller adoption; raster results are required together with existing fit/paint checks before the process-owned witness can pass.

## Tests
Immutable H7 baseline: 1,947 PASS (273.892 seconds inside unittest; separate baseline_exit.json records outer process timing). H8 atomic: 38 + 29 + 34 + 19 + 23 = 143 tests, plus 18 integration tests = 161 additions. Collected cumulative total: 2,108. Release is withheld until fresh extraction executes the complete suite; exact execution and archive/member hashes are recorded in the external `BIE_COMP_HARDENING_H8_FINAL_VERIFICATION.json` bound to the final ZIP. No test count alone proves product correctness.

All new task suites executed successfully. Independent Node controller tests use explicitly fake renderer packages and dummy media bytes, never a real-render claim. Installed tsc checks the instrumented helper with explicit test declarations, not complete pinned dependency compatibility.

## Browser evidence
Seven short technical source fixtures, 18 frames, 32 target-frame records and 132 PNGs. Five positive pixel cases pass; duplicate point/occluded-feature and subpixel polygon cases are correctly rejected. All frames in each fixture are captured; these are not sampled lesson videos. Captures also cover fade/reveal intentionally empty frames and selected Hindi/literal-text painting. Actual Chromium paints the generated DOM through explicit React/Remotion API doubles. `real_react=false`, `real_remotion=false`. The full viewport is inspected for each counterfactual target, but this is NOT arbitrary image-content understanding or complete cartographic/educational readability. Tiny map text in the technical fixture is not certified by a point-resolution pass; existing text/fit/contrast gates remain separate.

Seven source cases matched across fourteen independent processes. This proves source repeatability for those fixtures, not rendered-video determinism.

## Concrete coverage gaps (not new speculative features)
The source registry declares 18 element types and 16 actions. Current checked dispatch has 15 element emitters and 9 action names. Three missing elements: `shape`, `diagram`, `highlight`. Seven missing action names: `simulation_state`, `static_focus`, `static_trace`, `crossfade_states`, `state_snapshots`, `progressive_static_trace`, `path_endpoints_with_progress_marker`. Direct compiler/contract reproductions remain blocked. A separately named reduced-motion metadata variant does not prove that these registered incoming actions are consumed. Next work must implement source-faithful consumers or demonstrate an actually adopted lossless lowering. The inventory does not implement them, and present dispatch entries are not exhaustive property/combination certification.

## Actual execution boundaries
New technical source successfully publishes. Actual render entry stops at `FULL_TYPECHECK_BLOCKED` / missing pinned dependencies before the actual controller or render is started. Fresh npm lookup for inherited Remotion 4.0.506 returns `EAI_AGAIN`. No unsandboxed fallback or fake PNG/movie is used as release evidence. H7 kernel/strict-browser limitations are retained; dependency checks prevent reaching browser execution in this run.

## Workflow and continuation
This batch updates the existing gap ledger; it does not restart a full section re-audit. `BIE-COMP-H8-005` is the current checkpoint. Required consumer/lowering gaps, actual pinned compile/render and downstream book/teaching/game acceptance remain open. Section exit and product acceptance are false. No universal image understanding, language coverage, readability or teaching-equivalence claim is made.

## Packaging and verification
Current Integrated ZIP is separately downloadable and byte-identical to the one in Master. Master contains current deltas/evidence only, not recursive prior Masters. Large current PNGs/logs are in a separate evidence ZIP; small reproductions, summaries, fixtures/specs and essential source/tests are in the integrated workspace. No original font files are distributed. Exact changed originals and all parent hashes are preserved. The first development-interleaved baseline attempt was invalidated after a toolchain change; the subsequent immutable baseline run above is the valid baseline. Both logs are retained and only valid execution enters the summary.
