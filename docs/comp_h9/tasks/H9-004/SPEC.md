# BIE-COMP-H9-004 — Seven registered action consumers

## Governing finding
H8 live declared-consumer inventory, consolidated H3-R02. This is an audit-derived task, not a renumbered original-roadmap item.

## Producer / consumer implementation
Existing action names receive versioned contracts, actual consumers or checked source-preserving lowering. Fixed orthographic focus; full static trace; milestone static trace; typeset equation-state crossfade; analytic simulation time mapping; analytic state snapshots; static source path/endpoints with stationary numeric progress. Source actions remain unchanged in identities and receipts.

Implementation: `app/bie/compiler/registered_actions.py; app/bie/compiler/registered_action_compiler.py`. The existing DSL registry and original builders are not replaced.

## Bounded contract and non-claims
Schema bie.comp-registered-action.v1. Snapshot observations and every traced source vertex need bound references. Continuous model mapping retains original endpoints and checks oscillation sampling. This is not arbitrary simulation, proof checking, symbol-matched morphing, arbitrary state transformations, or verified learning equivalence.

## Reproduce
Restore the complete H9 integrated workspace, or all five ordered H9 deltas from the exact H8 baseline. Intermediate delta stages are source assembly only, not runnable releases.

```bash
PYTHONPATH=app:. PYTHONDONTWRITEBYTECODE=1 python -m unittest tests.compiler.test_comp_h9_004 -v
```

Suite discovery: 40 tests. Execution evidence is `TEST_RESULT.txt`; final extracted-package verification is external and bound to the archive SHA256. `accepted` remains false.
