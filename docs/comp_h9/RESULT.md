# COMP Hardening H9 — bounded declared-consumer implementation

Checkpoint: BIE-COMP-H9-005. Scope: cumulative DSL + COMP, not a claim about the live canonical GitHub repository. No repository write or commit.

## Implemented
The original diagram constructor has a backward-compatible keyword-only view_box extension; its old no-viewport calls retain their original props. Five audit-derived tasks implement the three missing element names and seven missing registered action names from H8. Shapes preserve all seven original kinds. Diagrams preserve explicit nodes, isolated nodes, relation direction and labels. Four highlight modes follow actual transformed target bounds on every frame. Seven actions have explicit source-bound contracts and real emitters or checked lowering. Existing compilation, capability checks, source maps, layout/conflict checking, reduced-motion resolution and source revalidation consume these implementations.

## Evidence boundaries
The final task suites contain 26, 24, 20, 40 and 18 tests (128 atomic), plus eight integration tests. The restored H8 baseline passed 2,108 tests. The final cumulative discovery is 2,244 tests. Delivery requires the separate `BIE_COMP_HARDENING_H9_FINAL_VERIFICATION.json`, bound to the exact integrated ZIP, documenting the fresh extraction/full-suite execution and ordered-delta restoration. That evidence is not replaced by discovery counts.

The final actual-Chromium diagnostic exercised 19 technical cases, 288 frames and 336 element-frame records, with 57 screenshots. All 19 technical content-fit outcomes passed. Components run using explicitly labelled React/Remotion API doubles. No actual Remotion render, pixel QA, educational quality, full visual readability or production browser isolation is certified by these cases. The independent source benchmark matched all 24 fixed cases (19 source passes, five intentional rejections) over 48 worker processes.

## Corrections and history
Local strict TypeScript checks exposed empty-array inference and literal-constant comparison errors; explicit types and numeric progress fixed those errors. A five-digit/hidden-alpha hex-color gap was closed with the explicitly bounded RGB color contract and separate opacity. The first full exploratory regression exposed ancestor-lineage lookup failures and capability-context mismatches. Exact ancestor bytes were restored. All 19 old generated source outputs remain byte-identical; a separate, explicit context-only baseline migration records the expanded capability registry. Three test/helper files have governed expectation/fixture-reference migrations; their original bytes are preserved. Historical fixtures and golden files are not rewritten.

## Remaining gates
Live dispatch reports 18/18 element names and 16/16 action names, with no remaining missing names. This does not mean all property combinations, arbitrary scene content or teaching equivalence are accepted. The actual checked-render attempt stopped at `FULL_TYPECHECK_BLOCKED: BLOCKED_DEPENDENCIES`; the npm probe returned `EAI_AGAIN`. Full pinned React/Remotion compilation, actual capture/rendered-frame QA, required locale/cartographic/educational evaluations and real-book tests remain unverified. Existing operational browser-profile constraints are not waived.

This batch updates the consolidated gap ledger; no new full section re-audit and no automatic section exit. No new hardening numbers are pre-invented. Read the ledger and required acceptance evidence before deciding the next governed action.

## Packaging
Separate integrated cumulative workspace and current Master Backup. Five ordered delta ZIPs require the exact H8 baseline and all five restored stages before suite execution. The Master embeds the current integrated ZIP byte-for-byte, not recursive prior Masters. New full browser/source-run evidence is separate from the active source workspace. Font files and node_modules are not included.

Status: COMP IN PROGRESS; section_exit_permitted=false; accepted=false.
