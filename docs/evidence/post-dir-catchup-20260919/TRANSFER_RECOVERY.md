# Transfer recovery and canonical preparation

The previous VIS text transfer at `1cae83b01352ea1bb84312a28ad49cc64aed88c0` is invalid. It contains incomplete Base64 groups and padding before later parts. Removing padding or weakening hash checks would not establish original-byte identity.

The failed parts and ready marker are removed from the active staging tree. Their exact historical bytes remain in Git history and the downloaded recovery artifact. No BIE source was extracted or adopted from that invalid transfer.

Recovery diagnostic GitHub run: **35468221575**, commit `772bc11a9e7efb3cf5dde055172e6a2acd50321f`, succeeded. It reran the exact DIR baseline preservation gates and **2,848 tests** with no failures, errors or skips.

## Local work completed

- Verified the complete supplied collection: 386 files, 193 top-level ZIPs; 435 nested ZIP objects and 83,616 member records.
- Preserved all supplied original bytes under content-addressed provenance paths, retaining original filenames and duplicate upload identities.
- Mapped original `app/bie` source into canonical `bie` without replacing the existing legacy alias adapter.
- Preserved existing baseline files, original tests, old Scene IR contract and before/after evidence for explicit path/import adaptations.
- Applied later COMP-owned diagram/image Scene IR amendments after the standalone DSL state.
- Added synthetic cross-section producer/consumer checks, including actual VIS -> ANI -> Scene IR -> checked compiler publication. No real-book or actual-render success is inferred.
- Ran the full local canonical regression: **6,485 tests / 993 files**, zero failures, errors or skips.
- Restored the sealed binary transport into a clean baseline and verified all **20,489 candidate files** byte-for-byte.
- Created four local phase commits against the exact restored DIR Git baseline and verified **6,346 final mapped Git blobs**. Those local commits have NOT been uploaded to GitHub.

## Publication boundary

The connected file-edit tools do not provide a local binary-file upload parameter. Large encoded data will not be transcribed through model text again. A single 8.45 MB browser upload to the staging folder triggers the installed sealed binary importer, which reruns cloud gates before staging publication. No source integration or `main` update is claimed until that succeeds and remote identities are read back.

## Explicit remaining availability record

Exact old `BIE_VIS_LAYOUT_001.zip` through `010.zip` bytes are absent from the supplied collection; their recorded hashes remain in the availability ledger. The supplied cumulative LAYOUT source and tests are preserved and present in the candidate. All supplied files are accounted for; complete historical archive availability is not claimed.

Original intake descriptions of missing entire VIS/ANI/DSL batches are superseded by this reconciled inventory. Do not ask for all sections again.
