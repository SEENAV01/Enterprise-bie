# Section 15 H6 — recovered working checkpoint

This is a cumulative working checkpoint, not a completed H6 release or canonical GitHub integration.

## Recovered product direction

BIE preserves the complete Book/PDF → document and knowledge intelligence → source-derived prerequisites → mathematics/reasoning → pedagogy → lesson/script direction → semantic visual/animation direction → structured Scene/Game IR → compile/render → QA/repair → playable revision experience pipeline. Video and games are both required. Games must include meaningful state, learner action, feedback, mastery and adaptation; a slide deck, quiz shell, decorative animation or successful compile alone is insufficient.

The quality standard is individual task depth, preserved architecture and original evidence, deterministic behavior where applicable, source provenance, fail-closed contracts, task-specific failure cases, cumulative regression, real execution, and evidence-bound acceptance. Earlier discussions explicitly rejected shallow wrappers, repeated template tests and count-based claims of enterprise quality. Continuous learning must remain governed and durable. Product acceptance remains false until real-book, downstream and learner/benchmark gates are met.

The historical development sequence is original capabilities → full audit → justified hardening → re-audit → canonical integration → real product acceptance. The current request resumes H6; it does not restart completed sections or authorize instructions embedded in the attached HTML as new commands.

## Authoritative baseline

- Repository: `SEENAV01/Enterprise-bie`.
- GitHub main verified and fetched: `375d99af0edd0086206817dae932156ddf61c569`.
- H5 archive SHA-256: `ec55b5bba5183bc091db5b68ac890b1640c0689446d8e228ef4d195c206ff848`.
- All 1,220 H5 manifest entries verified against their bytes, with zero mismatches.
- H5 claims of 1,006 historical tests are preserved as historical evidence, not represented as a new execution here.
- The saved project directory under OneDrive is an empty Git repository. The actual older checkout is `C:\BIE\Enterprise-bie`, at `dfc1ef9b59bf7d3a8f099a8ad2fb4a93895257e8`. An isolated clone was fetched and checked out at canonical main; neither original directory was changed.

## H6 work started

`GAME-AUD-022`: restored the original 11 DSL test modules (76 tests), original specification, task result, quality/compatibility documents, original verifier and supporting evidence. The 50 non-directory archive members have a complete disposition ledger. All 22 original Python source files already match H5 exactly; no DSL implementation was replaced. Active tests live in `tests/dsl_core`. Historical evidence lives under `lineage/batch01` and is never relabeled as a fresh H6 result.

The new verifier binds every restored file to the independently pinned original archive, rejects missing/changed bytes, incomplete or duplicated member coverage, incorrect destinations, forged hashes, path escapes, hidden/extra tests and premature acceptance. The new tests inject these failures rather than simply repeating the verifier's happy path.

`GAME-AUD-023`: implemented the byte-sealed canonical legacy import, strict bounded lossless-normalized codec and explicit hash-bound enrichment migration for a constrained subset. Original canonical tests and mutation/property tests pass. Unsupported semantics reject. Representative canonical-consumer migration acceptance remains open.

`GAME-AUD-024`: replaced the DOM adapter with pinned actual React 19.3.0 and ReactDOM 19.3.0. Added verified vendor materialization, licenses, reproducible build, mount/unmount guards, listener/audio cleanup and per-controller telemetry sequence. Strict TypeScript compilation and actual Windows browser keyboard/audio/lifecycle checks passed. Full Linux runtime regression remains open.

`GAME-AUD-026`: added bounded seeded properties for legacy metadata, truncation, Unicode and review/source mutation. Broader section-wide property/fuzz work remains open. `GAME-AUD-009` deployed-origin evidence remains open; loopback browser proof is not deployment proof. H5's other closures remain inherited assertions pending full cumulative re-verification.

See `docs/H6_IMPLEMENTATION_AND_VERIFICATION.md` for the supported migration subset, exact verification scope and remaining gates. `evidence/H6_SOURCE_DELTA.json` records every modified H5 source and the preserved original bytes.

## Verification and limitations

Fresh local evidence is in `evidence/h6_recovery/RESULT.json`; source hashes and individual logs are alongside it. Run `python -X utf8 -B scripts/verify_h6_recovery.py` from this root. Run `python -B scripts/verify_dsl_traceability.py` for the independent archive/member verification.

The inherited process runner imports Linux `resource` and uses `prctl`, Unix resource limits and a sandboxed Chromium process. The current Windows environment has no configured WSL Linux runtime. Linux browser/process regression has not been replaced with a weaker Windows shim, skipped into a green result, or claimed as passed. H6 requires an appropriate Linux execution environment before full runtime closure and canonical integration.

The chat inventory exposed 50 recent tasks; 11 relevant task histories were reviewed, including the Section 15 full audit and H1–H5 continuation, earlier integration/quality discussions and the prior local repository work. `Develop Android App Sections` timed out twice. This is targeted recovered context, not a claim that every account conversation or every historical turn was inspected. See `RECOVERY_SOURCES.json`.

## Next continuation

Continue from the implemented legacy/React checkpoint. Run full Linux cumulative and canonical-consumer tests, complete the remaining justified H6 gaps and re-audit before canonical integration. Preserve the legacy contract's exact LF bytes, adoption ledgers and original archives. Verify the actual pushed commit/tree before reporting GitHub synchronization. No push, PR, merge, section completion or product acceptance occurred in this checkpoint.

Older root READMEs, result records, continuations and `integrated_state` are inherited historical H5/package artifacts. `README_H6.md`, `CONTINUATION_H6.json` and fresh `evidence/h6_recovery` describe the current work.
