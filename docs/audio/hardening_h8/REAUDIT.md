# H8 Re-audit

H8 materially closes the remaining **implementation-side AUDIO-AUDIT-001-F03** items that were open after H7:
1. paid/remote-call ambiguity is durably journaled and fail-closed; no automatic duplicate POST after uncertain completion;
2. explicit evidence-bound operator resolution is required before a new paid attempt;
3. exact-origin/path egress authority and request-time environment credential binding are adopted into the live path without serializing secrets;
4. bounded host-local provider call admission and whole-workload preflight are implemented;
5. one inherited H6 summary-schema regression exposed by fresh H5 compatibility execution is repaired and covered.

Validation limits remain truthful: no real ElevenLabs call/listening, HSM/cloud secret-manager deployment or distributed multi-host scheduler was exercised. These are deployment/external-validation gates, not silently claimed passes.

Remaining material work before AUDIO section exit:
1. **F02** — production multilingual/accent/IPA/OOV evaluator authority, held-out calibration and independent/listening evidence.
2. **F04** — canonical DIR narration → AUDIO → ANI/COMP handoff, repair-driven dependent invalidation, pinned compile/render and rendered AV/caption evidence.
3. **AUDIO-FINAL-REGRESSION** — fresh complete AUDIO regression and final section re-audit after F02/F04 implementation work.

Section exit is not permitted.
