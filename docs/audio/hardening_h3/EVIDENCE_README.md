# Evidence use

External EVIDENCE.zip contains pristine baseline, final fresh extracted package tests, native durable benchmark output, exact dependency identities, and preserved development failures. Native sample sources are synthetic. TEST-ONLY public trust/runtime files are historical; their 600-second receipts are not fresh authorization. Private keys, model weights, native binaries and fonts are excluded. WAV examples are speech diagnostics, not a new production voice.
