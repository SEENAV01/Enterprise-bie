# BIE AUDIO Hardening H3 — durable local acoustic lifecycle

H3-001..005 implement the bounded durable acoustic lane under existing F03. F03 is not fully closed. No GitHub write, full section re-audit, runtime/product acceptance or live neural request.

The exact H2 parent was restored and rerun: 1129 tests, zero failures/errors/skips. The frozen H3 task suites executed 87 new tests, zero failures/errors/skips. Native English evaluation was executed against real delivered WAV, persisted through existing BIE CAS/SQLite envelopes and reloaded in a second independent CLI process with zero native evaluations and renewed signature verification. Hindi remains explicitly unsupported/BLOCKED. Source waveform bytes are unchanged. Final full fresh-extraction release results are in the separately supplied FINAL_VERIFICATION.json and FRESH_REGRESSION.txt; do not add historical totals without that execution evidence.

The five tasks have 17,24,19,15,12 tests. Atomic ZIPs are independently runnable shared-source/test closures, not ordered deltas. Five exact canonical dependency files supplement the five unchanged DIR fixtures; neither upstream class names nor their bytes are forked/replaced. The pinned APIs are from H2's original base commit, not a claim about today's main.

The full 413-file H2 baseline is accounted for: 409 unchanged, four metadata/manifest updates with exact originals retained. All inherited runtime, scripts, tests and dependency bytes are unchanged. Both earlier Batch 001 implementations remain.

The native benchmark exercised three English segments, 15 word windows and nine independent legacy acoustic searches. Cold reuse returned the same artifact ref and identical evidence/caption payloads. Random inherited publisher lock names and their outer manifests differ; each publication was independently verified, not claimed byte-identical as a whole. A raw stored-WAV corruption was rejected after a genuine inspector defect was reproduced and fixed. See CORRECTIONS.md for preserved development failures.

Fenced jobs include real killed-process reclaim, live-owner exclusion, heartbeat/cancellation, bounded attempts and completion-after-authentication. A crash between completed claim and lease can be recovered without native work. A crash before committing the pointer is at-least-once local evaluation, not exactly-once paid-provider execution; immutable orphan blobs may remain. Local service-owned storage, not distributed storage or power-loss certification, is the scope.

The original H2 bounded native subprocess remains; canonical kernel/network/secret-policy adoption across AUDIO is NOT implemented in this batch. F01 live neural/listening, F02 production multilingual/calibrated evaluator authority, wider F03 TTS/cache/MIX lifecycle and F04 actual DIR/ANI/COMP rendered AV are still open. Cryptographic authenticity of diagnostic reports is not pronunciation correctness. The synthetic-source benchmark uses ephemeral TEST-ONLY trust; keys are not shipped. Historical receipts expire and must not be reused as current release authorization.

Use the cumulative CONTINUATION.json and updated consolidated ledger; do not restart completed sections or auto-exit AUDIO.
