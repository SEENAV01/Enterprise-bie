# DIR section exit review — batch 014

## Verdict

**DIR IMPLEMENTATION-SCOPE COMPLETE — NOT ACCEPTED.**

The original 37 DIR tasks and 22 additive hardening tasks (fourteen prior, eight
in batch 014) now form the supported Director implementation. All 683 tests in 72
files pass together. Section exit authorizes lossless canonical integration; it
does not authorize product release or acceptance.

## Exit criteria

- Exact source bytes/provenance, RE closure, PED modes/objectives/assessments and
  optional governed teaching context reach one registered Director stage.
- Rich example, demonstration, inquiry/Socratic, misconception and notation
  obligations compile into separately reviewable grounded teaching.
- Complete source annotations and bounded hierarchical discourse/retrieval receive
  separately configured, conservatively aggregated review.
- Named-scene correction preserves unchanged work, then recompiles and reruns all
  global semantic/QA/review gates.
- Durable CAS indexing, idempotency, fenced crash recovery, current revisions and
  transitive invalidation are wired through the production composition root.
- Timing, visual-sync, animation-sync and game-handoff candidates are typed,
  revision-current and explicitly preview-only.
- External calibration has a source-pinned signed-oracle/provider-stack harness;
  absent real evidence records `NOT_RUN` rather than a synthetic PASS.

## Open evidence and downstream ownership

Real multi-domain PDFs/OCR, externally configured model quality, independent
expert benchmark sufficiency, authenticated grader deployment, representative
learner outcomes, rendered audio/video and playable GameIR/compiler/runtime QA
remain required before their respective acceptance gates. These are not silently
reclassified as complete. VIS/ANI and Game producers remain equally mandatory in
the product roadmap.

Canonical integration must preserve every prior archive and ledger row, apply the
eight atomic overlays in dependency order, run the combined repository gate and
verify exact remote commit/tree/blob state without force push.

