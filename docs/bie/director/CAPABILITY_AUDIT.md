# Current DIR capability audit — batch 014

Original checkpoint **BIE-DIR-QA-007 (37/37)**; additive checkpoint
**BIE-DIR-HARD-BENCHMARK-CALIBRATION-001**. The Director section is
**IMPLEMENTATION-SCOPE COMPLETE — NOT ACCEPTED**. The original product vision,
867-row ledger, prior ZIPs and architecture remain unchanged.

| Finding | Batch-014 resolution | Acceptance/downstream evidence still open |
| --- | --- | --- |
| DIR-AUDIT-CONTRACTS | All prior contract hardening remains; new rich, grade, hierarchy, correction, recovery, production and calibration records use strict known-field reconstruction. | Hostile-object exhaustiveness and external attestation remain empirical/security review. |
| DIR-AUDIT-SEMANTIC-TRANSPORT | Configured gateway execution, exact source/narration bindings, bounded retries and whole-result semantic QA remain on every fresh or corrected path. | Live provider truth/calibration is not demonstrated here. |
| DIR-AUDIT-PRODUCTION | One durable production composition root accepts only exact canonical RE/PED refs, registers the existing DIRECTOR stage and enforces current consumer/invalidation gates. | Production BI/OCR and upstream KI/PR/MATH/RE/PED deployments provide those refs; full VIS/ANI/Game stages own their executable outputs. |
| DIR-AUDIT-TEACHING | Worked examples, demonstrations, inquiry/Socratic probes, misconception correction and notation are exact source-bound typed obligations realized through PED/RE-bound scenes and spoken beats. Authenticated grades remain uncalibrated evidence and cannot waive prerequisites. | Representative learner effectiveness, curriculum/audience calibration and external grader operation require empirical evidence. |
| DIR-AUDIT-ANNOTATIONS | Complete source-scene calls plus hierarchical discourse leaves, every boundary, deterministic non-adjacent retrieval, independent aligned review and original global validation are implemented. | Multilingual/provider accuracy and extreme production distributions require calibration; indivisible over-budget scopes fail explicitly. |
| DIR-AUDIT-REPAIR-HANDOFF | Named-scene correction preserves the plan and unchanged scenes, recompiles/rechecks globally, safely reuses only unchanged local annotations, invalidates stale descendants and exposes typed timing/VIS/ANI/Game planning candidates. | Actual audio/video rendering and playable GameIR/compiler/runtime QA are downstream, not DIR claims. |
| DIR-AUDIT-RECOVERY | SQLite artifact journaling reloads verified CAS graphs; epoch-fenced leases reclaim exact crashed claims/revisions, heartbeat phase boundaries, prevent live-worker theft and replay completed results without provider calls. | Multi-host operational load/chaos testing remains deployment evidence. |
| DIR-AUDIT-EVALUATION | The existing independent-oracle benchmark is extended with exact source receipts, signed release verification, complete provider-stack identity, repeated stability gates and an explicit `NOT_RUN` path. | No independent expert-signed real-source release or external provider was available, so acceptance calibration is `NOT_RUN`. |
| DIR-AUDIT-CANONICAL | Lossless section integration is authorized after this audit and the combined gate; exact remote commit/tree/blob readback is required. | Product acceptance is never implied by repository integration. |

## Verification

**683 DIR tests / 72 files pass**, including all 600 batch-013 tests plus 83 new
completion tests. Zero failures, errors or skips. Focused production tests execute
the registered stage, durable restart/replay, crashed RUNNING-revision reclaim,
hierarchical annotation/review, scene correction, safe local reuse, current
consumers and upstream invalidation. Three-domain controlled calibration retains
independent oracles and rejects candidate self-scores.

The separate pre-integration canonical gate remains **2,160 tests / 551 files** at
commit `bc3147db408fc5a5d54017c8b907f580477ff2c3`. Packaging and integration must
re-run their own fresh gates. All 13,395 recorded canonical files and the original
867-row ledger are preservation inputs, not product-completion metrics.

## Exit verdict

The audit found no remaining DIR-side implementation gap required to hand exact,
review-only structured plans to VIS/ANI and Game producers. Missing live/expert/
real-source/learner/render/gameplay evidence is acceptance or downstream-stage
work and remains explicitly open. This verdict does not declare BIE, a generated
lesson, rendered media or a game accepted.

Read `COMPLETION_CONTRACTS.md` and `ADR-DIR-COMPLETION-014.md` for the exact
contract. Preserve video teaching and equally mandatory playable revision games,
shared learning structures, provenance, structured IR, replaceable tools/models,
uncertainty, bounded repair and governed cumulative learning.

