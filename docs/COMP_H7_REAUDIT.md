# COMP H7 — consolidated section re-audit

## Decision
**The eight H7 task contracts are implemented and locally tested. This is NOT a certificate that all remaining section-level hardening is complete. Section exit remains prohibited; product acceptance is false.**

The request was to close the remaining work in one consolidated pass. Substantial producer, integration and operational work was completed, but two broad inherited implementation findings cannot honestly be declared closed: universal/whole-ink visual QA and the unspecified broader specialized/state-transform coverage. Runtime blockers are separately identified; no acceptance result is invented. No automatic H8 numbering or unrelated feature expansion was added.

## Requirement-to-evidence assessment

| Finding | Delivered in this pass | Remaining boundary |
|---|---|---|
| R01 dynamic narration + layout repair | Same verified PCM assets traverse candidate compile, measured dynamic state/caption fit, selection, publication and replay. The previous H6 combination block is removed. Four measured candidate attempts select a valid layout without changing semantics/audio. | Real-platform measurements remain blocked. |
| R01 trusted render measurements | Production-only instrumented original Scene, pinned renderer, every-frame measurements/images, nonce-bound process witness, font/paint checks and pre/post identities. Caller JSON/test-double receipts cannot authorize. | Actual installed renderer execution has not occurred. Helper strict types/API behavior and media outputs still need verification under the pinned packages. |
| R01 visibility/ink/locale | Solid-color contrast, own/ancestor clipping, finite-point occlusion, opaque overlays, vector affine/stroke envelopes, font-ready/missing glyph diagnostics. Real Chromium positive/negative fixtures. | This is not exhaustive raster ink segmentation, universal shaping assessment, image interpretation or cartographic feature readability. Those original broader findings are not erased. |
| R02 specialized consumers | Actual equal-outline glyph affine correspondence, unmatched fade, reduced-motion discrete milestones for camera/equation/trace; existing state/audio consumers retained. | Correspondence is declared, not mathematical proof. Arbitrary 3D/state transforms and all unspecified specialized expectations are not certified. Semantic/learning equivalence requires actual expert/learner evidence. |
| R02 media adapters | Verified local MP3/FLAC/Vorbis/Opus/PCM decoder in kernel worker; explicit sample-clock PCM mixing and current narration descriptors. Actual MP3 run and sample fixtures. | Not TTS, spoken-transcript alignment, automatic ducking/mastering, or interactive game audio. |
| R03 operational controls | Real no-proc user/mount/PID/network namespaces, RO runtime, restricted writable paths, no-new-privileges, capabilities dropped, syscall deny policy, RLIMIT and lock concurrency; production math and decoder adoption. | Browser requires private procfs; this container denies that mount. No host-proc or unsandboxed fallback. Limits are per-process and tmpfs, not total cgroup accounting or proof against every kernel exploit. |
| R03 toolchain | Complete bounded installed file/lock identity, direct/transitive version match, internal symlink policy, Node/browser/native digests and before/after replay. | Full installed Remotion tree is unavailable. Synthetic package fixtures test the collector; they are not installed production dependencies. Local hashes are not publisher authenticity. |
| R04 real compile/render | Existing real-render entry uses isolated pinned typecheck, actual producer witness and sandboxed render/probe, with source/assets/deps revalidation. | `FULL_TYPECHECK_BLOCKED: BLOCKED_DEPENDENCIES`; npm `EAI_AGAIN`. Real discovery/smoke/full render: NOT RUN. |
| R05 product acceptance | No expansion of COMP into all other engines. | Real books, teaching quality, speech, playable games and enterprise acceptance remain open downstream. |

## Tests and evidence boundaries
Untouched H6 baseline: 1,761 tests passed. H7 targeted suite: 186 tests passed. The complete suite has 1,947 test cases; the external final-verification receipt records the fresh extracted release run and binds it to the integrated ZIP hash. An earlier full run found five lineage-preservation assertions before release metadata preparation; exact ancestor bytes were restored, not tests weakened. Historical test fixtures/goldens remain byte-identical.

Final diagnostic browser run: seven positive cases, 336 frames, 576 element-frame records, 21 screenshots. A separate dynamic repair measured four candidates (48 frames/96 records each) and selected candidate 003; those are distinct counts. Chromium is real; React/Remotion APIs are explicit test doubles. It does not produce a real-render witness. Source benchmark: nine fixed cases, 27 independent workers, all expectations/repetition matched (six positive, three rejected). Audio is synthetic technical tone, not speech.

## Actual faults corrected
Own-element clip ancestry was initially omitted; diagnostic now starts at the text container. Transparent structural overlay false positives were corrected while opaque pointer-events-none overlays remain blocked. Font codepoints classified unassigned are rejected even if a cmap contains them. Real isolated ffprobe exposed native alternatives symlinks; the required system alternatives directory is read-only, with no general host /etc exposure. New active-file edits exposed ancestral backup assertions; required original bytes were copied from known ancestor hashes rather than rewriting the assertions. Failed development receipts are retained in the evidence bundle.

## Scope and next action
Do not label every codec/simulation/game feature a compiler requirement; equally do not call required broader visual/state coverage optional merely to exit. Preserve the inherited R01/R02 broad residuals until their exact capability acceptance matrix is implemented or approved ownership is recorded. For R03/R04, the next decisive execution is the existing harness on an environment with the exact pinned packages and private-proc browser namespace policy. No source-only or caller-supplied PASS can substitute.

The machine-readable closure register explicitly retains `OPEN_IMPLEMENTATION` and `BLOCKED_ENVIRONMENT`. H7 is the local checkpoint, not canonical GitHub integration. No GitHub write, commit, external publish or acceptance was performed.
